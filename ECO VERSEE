<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>EchoVerse — Audiobook Creator (Frontend)</title>
  <style>
    :root{
      --bg:#0f1724; --card:#0b1220; --accent:#7c5cff; --muted:#9aa4b2; --glass:rgba(255,255,255,0.03);
      --radius:12px; --gap:16px; font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }
    *{box-sizing:border-box}
    body{margin:0; min-height:100vh; background:linear-gradient(180deg,#081022 0%, #071227 100%); color:#e6eef8; padding:28px;}

    .app{max-width:1100px;margin:0 auto;display:grid;grid-template-columns:1fr 420px;gap:24px;align-items:start}
    header{grid-column:1/-1;display:flex;align-items:center;gap:16px;margin-bottom:6px}
    .logo{width:56px;height:56px;border-radius:10px;background:linear-gradient(135deg,var(--accent),#3ec7c7);display:flex;align-items:center;justify-content:center;font-weight:700;color:#041024}
    h1{font-size:20px;margin:0}
    p.lead{margin:0;color:var(--muted);font-size:13px}

    /* Left column */
    .card{background:var(--card);padding:18px;border-radius:var(--radius);box-shadow:0 6px 18px rgba(2,6,23,0.6);}
    .upload-area{border:1px dashed rgba(255,255,255,0.06);padding:18px;border-radius:10px;background:var(--glass);display:flex;flex-direction:column;gap:12px}
    .controls{display:flex;gap:12px;flex-wrap:wrap}

    input[type=file]{display:none}
    .btn{background:linear-gradient(90deg,var(--accent),#46d6b1);border:none;padding:10px 14px;border-radius:10px;color:#021426;font-weight:600;cursor:pointer}
    .btn.ghost{background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted)}

    .preview{height:320px;overflow:auto;background:transparent;padding:12px;border-radius:8px;border:1px solid rgba(255,255,255,0.02)}
    .meta{display:flex;justify-content:space-between;align-items:center;margin-top:10px}

    /* Right column */
    .settings{display:flex;flex-direction:column;gap:12px}
    label{font-size:13px;color:var(--muted)}
    select,input,textarea{width:100%;padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:inherit}
    .row{display:flex;gap:12px}
    .small{width:120px}

    .progress{height:10px;background:rgba(255,255,255,0.04);border-radius:999px;overflow:hidden}
    .progress > i{display:block;height:100%;background:linear-gradient(90deg,var(--accent),#46d6b1);width:0%}

    footer{grid-column:1/-1;text-align:center;margin-top:18px;color:var(--muted);font-size:13px}

    /* responsive */
    @media (max-width:980px){
      .app{grid-template-columns:1fr; padding:18px}
      .logo{width:48px;height:48px}
    }
  </style>
</head>
<body>
  <div class="app">
    <header>
      <div class="logo">EV</div>
      <div>
        <h1>EchoVerse — Audiobook Creator</h1>
        <p class="lead">Upload text, preview narration, and export chapterized audio (frontend). Works with a synth backend endpoint.</p>
      </div>
    </header>

    <main class="card">
      <section class="upload-area">
        <div style="display:flex;gap:12px;align-items:center;justify-content:space-between">
          <div>
            <strong>Upload source text</strong>
            <div style="font-size:13px;color:var(--muted)">Supports .txt, .md (EPUB/PDF support via backend)</div>
          </div>
          <div>
            <label class="btn" for="fileInput">Choose file</label>
            <input id="fileInput" type="file" accept=".txt,.md" />
          </div>
        </div>

        <div class="controls">
          <button class="btn" id="pasteBtn">Paste text</button>
          <button class="btn ghost" id="clearBtn">Clear</button>
          <div style="margin-left:auto;display:flex;gap:8px;align-items:ce…
